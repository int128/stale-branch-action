name: stale-branch-action
description: Clean up stale branches in GitHub Actions

inputs:
  token:
    description: GitHub token
    required: true
    default: ${{ github.token }}

runs:
  using: 'node16'
  main: 'dist/index.js'
